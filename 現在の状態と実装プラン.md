# 現在の状態と実装プラン

更新日: 2026-09-19（日本時間）  
作業フォルダ: `C:\develop\codex-deck`  
参照元: `C:\develop\cockpit`（参照専用）

## 現在の状態

- アプリ本体は未実装。M0〜M6の13タスクと受け入れ条件は定義済み。
- 開発方式は[ADR-0002](docs/adr/0002-native-workflow.md)に従い、会話中のメインエージェントが内蔵サブエージェントを指揮する方式へ変更した。実行指示は[WORKFLOW.md](docs/harness/WORKFLOW.md)。
- [小規模試作](prototypes/workflow-mini/RESULT.md)では実装、独立レビューPASS、意図的な欠陥の検出とFAIL、元の実装者によるFIX、再検証と再レビューPASSを実施した。試作は親モデルを継承しており、Sol/Astraの役割別切り替えやアプリ実機ゲートの証拠ではない。
- 旧CLIのsmokeは通常のPowerShellで成功した。旧CLIのM0-01実行状態はソケット10013で停止しており、`cleanupRequired` が残る。新方式への変更を理由に解除しない。詳細は[検証記録](docs/harness-validation.md)。
- 新方式の文書はSol highの4観点独立レビューとFIX後の再レビューを完了し、全観点PASS。結果は[再設計レビュー記録](docs/harness/redesign-review.md)。これは文書の判定であり、アプリ実装の開始・合格を意味しない。

## 実装の進め方

アプリ実装の依頼を受けたメインエージェントは、[AGENTS.md](AGENTS.md)、[製品仕様](docs/spec/product.md)、対象タスクと依存関係を[milestones/tasks.json](milestones/tasks.json)で確認する。ハーネスの手順と役割は[WORKFLOW.md](docs/harness/WORKFLOW.md)、[roles.md](docs/harness/roles.md)、[review-rubric.md](docs/harness/review-rubric.md)に従う。

メインが対象タスクを実装者へ割り当て、受け入れチェックを実行し、独立レビュワーの判定を集約する。FAILは元の実装者へFIXを戻し、必要な検証とレビューをやり直す。M0の実機接続ゲートを満たすまでM1へ進まない。実機ゲートを実施できない場合は未検証として停止し、証拠を作らない。進行記録は `.harness/native/<run-id>/` に追記し、必須の実機証拠・検証ログは `.harness/evidence/<task>/` に保存する。

## 旧CLI方式の記録（2026-09-18〜19）

旧ハーネスのCLI実装、模擬テスト、smokeと失敗履歴は削除していない。旧方式の操作説明は[tools/harness/README.md](tools/harness/README.md)、設計経緯は[ADR-0001](docs/adr/0001-harness.md)に残る。旧 `.harness/state.json` のM0-01は `blocked` で、PID 7488の子孫終了未確認を示す `cleanupRequired.treeConfirmed: false` が残る。これは新方式の合格や再開可能性を意味しない。

参照元cockpit、認証情報、ユーザーのCodex設定は変更しない。自動commit/push/reset/clean/stashは行わない。
